# HTTP client
requests>=2.31.0

# Camera and image processing
opencv-python>=4.8.0
Pillow>=10.0.0

# Audio input/output (optional for text mode)
pyaudio>=0.2.13
# pygame>=2.5.0  # Using system package python3-pygame

# Audio processing and noise reduction
scipy>=1.11.0
numpy>=1.24.0
# webrtcvad>=2.0.10  # Optional - commented out because it requires C++ compilation

# Raspberry Pi camera (uncomment if using Pi camera module)
# picamera2>=0.3.0

# Wake word detection
pvporcupine>=3.0.0
vosk>=0.3.45

# Local speech-to-text (optional). Used by client_vosk.py when
# config.json has "stt_engine": "local". Falls back to cloud STT
# if not installed, so removing this won't break existing setups.
faster-whisper>=1.0.0

# Local text-to-speech (optional). Used by client_vosk.py when
# config.json has "tts_engine": "local". Falls back to cloud TTS
# if not installed or if the voice model is missing.
piper-tts>=1.2.0

# Configuration
python-dotenv>=1.0.0
